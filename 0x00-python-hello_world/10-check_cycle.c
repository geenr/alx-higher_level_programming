#include "list.h"

/**
 * check_cycle -  function that checks if singly linked list has a cycle in it
 * @list: singly linked list to be checked
 *
 * Return: 0 for no cycle, 1 for a cycle
 */

int check_cycle(listint_t *list)
{
	return (0);
}
